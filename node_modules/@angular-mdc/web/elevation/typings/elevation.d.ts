import { ElementRef, OnChanges, Renderer2, SimpleChange } from '@angular/core';
export declare class MdcElevation implements OnChanges {
    private _renderer;
    elementRef: ElementRef;
    private _mdcElevation;
    mdcElevation: number;
    constructor(_renderer: Renderer2, elementRef: ElementRef);
    ngOnChanges(changes: {
        [key: string]: SimpleChange;
    }): void;
}
