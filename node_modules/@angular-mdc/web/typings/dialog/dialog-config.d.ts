import { ViewContainerRef, InjectionToken } from '@angular/core';
/** Valid ARIA roles for a dialog element. */
export declare type DialogRole = 'dialog' | 'alertdialog';
export declare const MDC_DIALOG_DATA: InjectionToken<any>;
export declare class MdcDialogConfig {
    /** ID for the dialog. If omitted, a unique one will be generated. */
    id?: string;
    /** The ARIA role of the dialog element. */
    role?: DialogRole;
    /** ID of the element that describes the dialog.  */
    ariaDescribedBy?: string | null;
    /** Aria label to assign to the dialog element */
    ariaLabel?: string | null;
    /** Whether the user can use escape key to close the dialog */
    escapeToClose?: boolean;
    /** Whether the user can click outside to close the dialog */
    clickOutsideToClose?: boolean;
    /** Data being injected into the child component. */
    data?: any;
    /** The view container to place snack bar into. */
    viewContainerRef?: ViewContainerRef;
}
