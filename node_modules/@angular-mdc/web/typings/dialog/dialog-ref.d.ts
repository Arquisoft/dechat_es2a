import { OverlayRef } from '@angular-mdc/web/overlay';
import { MdcDialogContainer } from './dialog-container';
import { Observable } from 'rxjs';
/**
 * Reference to a dialog dispatched from the MdcDialog service.
 */
export declare class MdcDialogRef<T, R = any> {
    private _overlayRef;
    _containerInstance: MdcDialogContainer;
    readonly id: string;
    componentInstance: T;
    /** Subject for notifying the user that the dialog has finished opening. */
    private _afterOpen;
    /** Subject for notifying the user that the dialog has finished closing. */
    private _afterClosed;
    /** Subject for notifying the user that the dialog has started closing. */
    private _beforeClose;
    /** Result to be passed to afterClosed. */
    private _result;
    constructor(_overlayRef: OverlayRef, _containerInstance: MdcDialogContainer, id?: string);
    /** Closes the dialog. */
    close(dialogResult?: R): void;
    /**
     * Gets an observable that is notified when the dialog is finished opening.
     */
    afterOpen(): Observable<void>;
    /**
     * Gets an observable that is notified when the dialog is finished closing.
     */
    afterClosed(): Observable<R | undefined>;
    /**
     * Gets an observable that is notified when the dialog has started closing.
     */
    beforeClose(): Observable<R | undefined>;
}
