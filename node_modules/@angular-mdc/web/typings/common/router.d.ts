import { AfterContentInit, EventEmitter, OnDestroy, QueryList } from '@angular/core';
import { Router, RouterLink, RouterLinkWithHref } from '@angular/router';
export interface MdcRouteEvent {
    active: boolean;
}
export declare class MdcRouter implements AfterContentInit, OnDestroy {
    private _router;
    /** Emits whenever the component is destroyed. */
    private _destroy;
    /** Event emitted when the link is set active. */
    readonly routeChange: EventEmitter<MdcRouteEvent>;
    _routerlinks: QueryList<RouterLink>;
    _routerHrefLinks: QueryList<RouterLinkWithHref>;
    constructor(_router: Router);
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    private _listenToLinkChanges();
    private _listenToHrefLinkChanges();
    private _refreshActiveLink();
    private _isRouterActivated();
    private _linkState();
    private isLinkActive();
    /** Emits an activated link event. */
    private _emitActivated(active);
}
export declare class MdcRouterModule {
}
