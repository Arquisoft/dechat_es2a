import { ElementRef, OnDestroy, OnInit, Renderer2 } from '@angular/core';
export declare class MdcTextFieldHelperText implements OnInit, OnDestroy {
    private _renderer;
    elementRef: ElementRef;
    id: string;
    persistent: boolean;
    validation: boolean;
    isHostClass: boolean;
    ariaHidden: string;
    readonly classPersistent: string;
    readonly classValidation: string;
    private _mdcAdapter;
    foundation: {
        init(): void;
        destroy(): void;
        setContent(content: string): void;
        showToScreenReader(): boolean;
        setValidity(inputIsValid: boolean): void;
        setPersistent(isPersistent: boolean): void;
        setValidation(isValidation: boolean): void;
    };
    constructor(_renderer: Renderer2, elementRef: ElementRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    destroy(): void;
    /** Sets the content of the helper text field. */
    setContent(content: string): void;
    /** Sets the validity of the helper text based on the input validity. */
    setValidity(validity: boolean): void;
    /** Makes the helper text visible to the screen reader. */
    showToScreenReader(): void;
    /** Sets the persistency of the helper text. */
    setPersistent(isPersistent: boolean): void;
    /** True to make the helper text act as an error validation message. */
    setValidation(isValidation: boolean): void;
}
