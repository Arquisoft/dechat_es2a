import { ChangeDetectorRef, ElementRef, Renderer2 } from '@angular/core';
export declare class MdcShapeContainerCorner {
    private _changeDetectorRef;
    elementRef: ElementRef;
    private _renderer;
    isHostClass: boolean;
    corner: string;
    private _corner;
    constructor(_changeDetectorRef: ChangeDetectorRef, elementRef: ElementRef, _renderer: Renderer2);
    setCorner(corner: string): void;
    /** Retrieves the DOM element of the component host. */
    private _getHostElement();
    /** Make sure the corner value is supported. */
    protected _isCornerValid(corner: string): boolean;
}
export declare class MdcShapeContainer {
    elementRef: ElementRef;
    isHostClass: boolean;
    constructor(elementRef: ElementRef);
}
