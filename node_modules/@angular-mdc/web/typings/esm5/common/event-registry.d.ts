export declare class EventRegistry {
    private unlisteners;
    constructor();
    listen(type: string, listener: EventListener, target: Element | Window | Document, passive?: any): void;
    unlisten(type: string, listener: EventListener): void;
}
