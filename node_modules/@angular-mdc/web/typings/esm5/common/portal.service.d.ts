import { ApplicationRef, ComponentFactoryResolver, ComponentRef, Injector } from '@angular/core';
export declare class MdcPortalService {
    private _componentFactoryResolver;
    private _appRef;
    private _injector;
    /** A function that will permanently dispose portal host. */
    private _disposeFn;
    constructor(_componentFactoryResolver: ComponentFactoryResolver, _appRef: ApplicationRef, _injector: Injector);
    createComponentRef(component: any, parent?: HTMLElement): ComponentRef<any>;
    private _getComponentRootNode(componentRef);
    private _addChild(child, parent);
    setDisposeFn(fn: () => void): void;
    dispose(): void;
    private _invokeDisposeFn();
}
