import { OnDestroy } from '@angular/core';
import { MdcPortalService } from '@angular-mdc/web/common';
import { MdcSnackbarRef } from './snackbar-ref';
import { MdcSnackbarComponent } from './snackbar.component';
import { MdcSnackbarConfig } from './snackbar-config';
export declare class MdcSnackbar implements OnDestroy {
    private _portalService;
    /** Emits whenever the component is destroyed. */
    private _destroy;
    private _openedSnackbarRef;
    /** Subscription to snackbar. */
    private _snackbarSubscription;
    constructor(_portalService: MdcPortalService);
    ngOnDestroy(): void;
    /**
       * Shows a snackbar with a message and an optional action.
       * @param message The message to show in the snackbar.
       * @param actionText The label for the snackbar action.
       * @param config Additional configuration options for the snackbar.
       */
    show(message: string, actionText?: string, config?: MdcSnackbarConfig): MdcSnackbarRef<MdcSnackbarComponent>;
    isShowing(): boolean;
    /**
     * Dismisses the currently-visible snack bar.
     */
    dismiss(): void;
}
