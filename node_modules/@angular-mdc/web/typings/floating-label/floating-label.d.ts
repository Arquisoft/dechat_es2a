import { ElementRef, OnDestroy, OnInit, Renderer2 } from '@angular/core';
import { EventRegistry } from '@angular-mdc/web/common';
export declare class MdcFloatingLabel implements OnInit, OnDestroy {
    private _renderer;
    elementRef: ElementRef;
    private _registry;
    isHostClass: boolean;
    private _mdcAdapter;
    foundation: {
        init(): void;
        destroy(): void;
        getWidth(): number;
        shake(shouldShake: boolean): void;
        float(shouldFloat: boolean): void;
    };
    constructor(_renderer: Renderer2, elementRef: ElementRef, _registry: EventRegistry);
    ngOnInit(): void;
    ngOnDestroy(): void;
    destroy(): void;
    /** Returns the width of the label element. */
    getWidth(): number;
    /** Styles the label to produce the label shake for errors. */
    shake(shouldShake: boolean): void;
    /** Styles the label to float or dock. */
    float(shouldFloat: boolean): void;
    /** Retrieves the DOM element of the component host. */
    private _getHostElement();
}
